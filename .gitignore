# Local state / artifacts
*.kubeconfig
kubeconfig-*
.DS_Store
charts/tenant/charts/
charts/tenant/Chart.lock
*.tgz
*.pdf

# Markdown docs: not published by default (which ones to include is decided later).
# To include a specific one: git add -f <file>.md  (or drop this rule).
*.md
!README.md

# Orphan diagrams — kept locally, NOT shipped (not referenced in README)
architecture-ideal-vs-homelab.svg
architecture.mermaid
flow-deploy.mermaid
flow-management-of-managements.mermaid
flow-management-of-managements.svg
flow-real-openstack.svg
helm-vs-crossplane.svg
overview-simplified.svg

# Scratch recording (dup of showcase-platform) — kept locally, not shipped
full-read-test.cast

# GitHub Pages blog (docs/) — DO publish these markdown files
!docs/*.md
!docs/**/*.md
