# Tenant in prod, region eu-west. Per-region placement materializes the blast
# radius model (ADR-11): one host per region / sovereignty domain.
tenant:
  name: tenant-a
  environment: prod
  domain: eu-west.example.com
applications:
  api:
    version: "2.23.0"     # prod can lag one version behind (controlled release)
  web:
    version: "1.26-alpine"
