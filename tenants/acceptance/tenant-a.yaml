# Same tenant promoted to acceptance, with potentially different versions.
tenant:
  name: tenant-a
  environment: acceptance
  domain: acc.example.local
applications:
  api:
    version: "2.23.1"
  web:
    version: "1.27-alpine"
